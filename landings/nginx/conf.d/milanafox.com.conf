server {
    listen 80;
    server_name milanafox.com;
    root /var/www/milanafox.com;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # For Let's Encrypt HTTP challenge
    location /.well-known/acme-challenge/ {
        root /var/www/milanafox.com;
    }
}

