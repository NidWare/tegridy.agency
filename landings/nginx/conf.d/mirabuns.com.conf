server {
    listen 80;
    server_name mirabuns.com;
    root /var/www/mirabuns.com;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # For Let's Encrypt HTTP challenge
    location /.well-known/acme-challenge/ {
        root /var/www/mirabuns.com;
    }
} 